version: '3.9'

services:

  db:
    image: postgres
    container_name: "post-postgres-db"
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5432:5432"

  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080